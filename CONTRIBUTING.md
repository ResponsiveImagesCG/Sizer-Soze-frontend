# Contributing to Sizer Soze

## Getting started 
To get started fork and clone this repo. On your local machine be sure to run `npm install` and `bower install` to download all the projects dependencies. 

## Grunt tasks
